# Drawbridge Setup for Cleenly

## ✅ Что уже настроено:

1. ✅ Файлы Drawbridge скопированы в проект
2. ✅ Создан `.cursorrules-drawbridge` с правилами AI
3. ✅ Создан workflow `/bridge` для обработки задач
4. ✅ Созданы файлы `moat-tasks.md` и `moat-tasks-detail.json`
5. ✅ Создана папка `screenshots/` для скриншотов
6. ✅ Добавлены исключения в `.gitignore`

## 🔧 Что нужно сделать вручную:

### 1. Установить Chrome расширение

1. Откройте Chrome и перейдите на `chrome://extensions/`
2. Включите **Developer mode** (переключатель в правом верхнем углу)
3. Нажмите **Load unpacked** (Загрузить распакованное расширение)
4. Выберите папку: `/Users/ekrasnoperov/Desktop/drawbridge/chrome-extension`
5. Расширение Drawbridge появится в списке
6. Закрепите его на панели инструментов (Pin)

### 2. Подключить проект Cleenly

1. Откройте `http://localhost:3000` в Chrome
2. Кликните на иконку Drawbridge в панели инструментов
3. Нажмите **Connect**
4. Выберите папку проекта: `/Users/ekrasnoperov/GITHUB-PROJECT/cleenly-app/cleenly`
5. Разрешите Drawbridge доступ к файлам

### 3. Начать работу

#### Режим комментариев (Comment Mode):
- Нажмите `C` на клавиатуре
- Наведите курсор на элемент страницы
- Кликните и напишите комментарий
- Нажмите Enter или Submit

#### Режим рисования (Rectangle Mode):
- Нажмите `R` на клавиатуре
- Нарисуйте прямоугольник вокруг области
- Напишите комментарий
- Нажмите Enter или Submit

#### Обработка задач в Cursor:
```bash
# Безопасный режим (по одной задаче)
/bridge

# Пакетный режим (группы задач)
batch bridge

# Автоматический режим (все задачи сразу)
yolo bridge
```

## 📝 Примеры использования для Cleenly:

### Пример 1: Изменить цвет кнопки
1. Откройте `http://localhost:3000`
2. Нажмите `C`
3. Кликните на кнопку "Book Now"
4. Напишите: "Сделать кнопку более яркой, использовать #FF6B35"
5. В Cursor запустите `/bridge`

### Пример 2: Настроить отступы в Hero секции
1. Нажмите `R`
2. Выделите область заголовка
3. Напишите: "Увеличить отступ сверху на 20px"
4. В Cursor запустите `/bridge`

### Пример 3: Изменить размер логотипа
1. Нажмите `C`
2. Кликните на логотип CLEENLY
3. Напишите: "Увеличить размер шрифта до 24px"
4. В Cursor запустите `/bridge`

## 🎯 Горячие клавиши:

- `C` - Режим комментариев
- `R` - Режим рисования прямоугольников
- `Esc` - Выход из режима
- `Enter` - Отправить комментарий

## 📂 Файлы проекта:

- `.cursorrules-drawbridge` - Правила AI для обработки задач
- `.agent/workflows/bridge.md` - Workflow для команды /bridge
- `moat-tasks.md` - Список задач (читаемый формат)
- `moat-tasks-detail.json` - Детали задач (JSON)
- `screenshots/` - Скриншоты аннотаций
- `moat-watcher.js` - Скрипт для отслеживания изменений

## 🚀 Следующие шаги:

1. Установите Chrome расширение (см. инструкцию выше)
2. Подключите проект Cleenly
3. Создайте первую аннотацию на странице
4. Запустите `/bridge` в Cursor
5. Наблюдайте, как AI применяет изменения!

## 💡 Советы:

- Начните с простых задач (изменение цвета, размера)
- Используйте `step bridge` для первых экспериментов
- Группируйте похожие задачи для эффективности
- Всегда проверяйте изменения перед коммитом

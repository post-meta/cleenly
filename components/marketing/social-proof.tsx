// Disabled: testimonials deferred until real customer reviews exist.
// Per project policy (CLAUDE.md): no fake testimonials.
// File kept as placeholder; remove via `git rm` when convenient.
export function SocialProof() {
  return null;
}

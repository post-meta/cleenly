// Disabled: previously displayed fabricated metrics (1200+ cleanings, 4.9★, 98% rebook).
// Kept as null component until real metrics exist. Per project policy: no fabricated stats.
// Remove via `git rm` when convenient.
export function ImpactBar() {
  return null;
}

// Referral economics — single source of truth. Give $25, get $25.
export const REFERRAL_REWARD_CENTS = 2500; // to the referrer, after friend's 1st completed cleaning
export const REFERRAL_REFEREE_DISCOUNT_CENTS = 2500; // off the new customer's first cleaning
export const REFERRAL_REWARD_DOLLARS = REFERRAL_REWARD_CENTS / 100;
